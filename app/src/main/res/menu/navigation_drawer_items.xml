<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:checkableBehavior="single" android:id="@+id/navGroupTop">
        <item android:id="@+id/nav_switch"
            app:actionLayout="@layout/action_view_switch"
            android:title="Sofort essen" />
        <item android:title="Filter">
            <menu>
                <group android:checkableBehavior="single">
                    <item
                        android:id="@+id/nav_radius"
                        android:icon="@drawable/ic_radius"
                        android:title="Umkreis"/>
                </group>
            </menu>
        </item>
    </group>

    <group android:checkableBehavior="single" android:id="@+id/navGroupBottom">
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_phone"
            android:title="Einstellungen"/>
    </group>
</menu>